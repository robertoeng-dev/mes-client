# Ferramentas de autor — nao sao necessarias para rodar o cliente na estacao.
#   pip install -r requirements-dev.txt
-r requirements.txt

python-docx==1.2.0   # usado apenas por gerar_documentacao.py
pytest==8.3.4        # suite de testes
